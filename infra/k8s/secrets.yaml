# Example: Syncing Google Secret Manager secrets to Kubernetes Secrets
# Using External Secrets Operator (Standard in GCP optional)
apiVersion: v1
kind: Secret
metadata:
  name: jurisnexo-secrets
type: Opaque
stringData:
  # Placeholder: In prod, utilize ExternalSecrets or mount manually
  DATABASE_URL: "postgresql://user:pass@10.x.x.x:5432/jurisnexo"
  REDIS_URL: "redis://10.x.x.x:6379"
  JWT_SECRET: "changeme"
